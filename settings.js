'use strict';

// Damit die Settings in allen Modulen angewandt werden können, 
// werden diese mithilfe der settings.js geshared.

const settings = {
    imgHeight: 400,
    amp:20,
}

export default settings;